import React, { Component } from 'react';
// import d3 modules here
// in the command line in the block_to_react dir, run `npm install --save [d3-module-name]`

class AxisBottom extends Component {
    constructor(props) {
        super(props);

        // add initial state here
        // bind this context here
        // create refs here
    }

    // add React lifecycle methods here (ex: componentDidMount())

    // add any class methods here

    render() {
        return (
            // return jsx here
            <div/>
        );
    }
}

export default AxisBottom;
